/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  images: {
    domains: ["shinharadam.sitecoresandbox.cloud", "images.unsplash.com"],
  },
};
